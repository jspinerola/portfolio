---
const { lowercase = false, animated = false, xl = false } = Astro.props;
const sizeClass = xl
  ? "text-4xl md:text-5xl lg:text-6xl"
  : "text-2xl md:text-3xl lg:text-4xl";
---

<h1
  class={`${sizeClass} font-black font-heading uppercase mb-4 ${animated ? " isAnimated" : ""}`}
  style={`
    ${lowercase ? "text-transform: none;" : ""}
    ${animated ? "opacity: 0;" : ""}
  `}
>
  <slot />
</h1>
